import { Text, View } from "react-native";
import { theme } from "../../../../../App";

export const ImageClient = () => {
  return (
    <View style={{ flex: 1, backgroundColor: theme.colors.default }}>
      <Text>imageClient</Text>
    </View>
  );
};
